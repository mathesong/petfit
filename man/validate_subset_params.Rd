% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation.R
\name{validate_subset_params}
\alias{validate_subset_params}
\title{Validate Subsetting Values Against the Available Data}
\usage{
validate_subset_params(data, subset_params)
}
\arguments{
\item{data}{A data frame to be subset — the combined TACs table, or any table
carrying the subsetting columns.}

\item{subset_params}{Named list of subsetting values, as built by
\code{\link[=parse_semicolon_values]{parse_semicolon_values()}}. Names must be drawn from \code{sub}, \code{ses}, \code{task},
\code{trc}, \code{rec}, \code{run} and \code{regions}. A \code{negate} attribute on a value marks
that field as an exclusion.}
}
\value{
Invisibly \code{TRUE} when every included value matches. Otherwise an
error naming every offending field and listing the values available in each.
}
\description{
Check that every subsetting value matches at least one row of
the data it will be used to filter.

petfit's subsetting fields are free text, so a typo or a mistyped separator
used to filter silently: the offending value simply matched nothing, the
analysis ran on a narrower dataset than requested, and reports were
generated as though nothing were wrong.

Included and excluded values are treated differently, deliberately. A value
you asked to \emph{include} that matches nothing silently narrows the analysis,
so it is an \strong{error}. A value you asked to \emph{exclude} that matches nothing
leaves the analysis complete rather than wrong, so it is a \strong{warning} — but
it must not pass in silence, because you believe you removed something you
did not.
}
