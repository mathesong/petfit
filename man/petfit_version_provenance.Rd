% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/version_provenance.R
\name{petfit_version_provenance}
\alias{petfit_version_provenance}
\alias{petfit_generated_by}
\alias{petfit_check_version}
\alias{petfit_check_combined_tacs_version}
\title{Record and Check Which Version of petfit Produced Something}
\usage{
petfit_generated_by()

petfit_check_version(
  provenance,
  what,
  notify = function(msg, type) {
 }
)

petfit_check_combined_tacs_version(
  combined_tacs_file,
  notify = function(msg, type) {
 }
)
}
\arguments{
\item{provenance}{A parsed configuration or sidecar, as a list.}

\item{what}{Short description of the file, used in the message.}

\item{notify}{Notification callback taking \verb{(message, type)}.}

\item{combined_tacs_file}{Path to \code{desc-combinedregions_tacs.tsv}.}
}
\value{
For \code{petfit_generated_by()}, a list with \code{Name} and \code{Version}.

For \code{petfit_check_version()}, invisibly \code{TRUE} when the version is
current and \code{FALSE} when it is older or absent.
}
\description{
Helpers for stamping the running version onto the files petfit
writes, and for warning when a later step meets a file an older version
wrote.

The reason is concrete. A measurement's identifier -- the \code{pet} column, and
the filename stem built from it -- used to be derived from whichever BIDS
entities happened to vary across the cohort, so it moved when the cohort
changed. It is now built from each measurement's own entities. A
\code{desc-combinedregions_tacs.tsv} written before that change carries the old
identifiers, and a step that constructs the new ones from the same file's
path will not match them. Nothing about the older file looks wrong; it
simply disagrees.

The remedy is always the same -- re-run every step, region definition
included, with the current version -- so these functions warn and say so
rather than stopping.
}
\section{Functions}{
\itemize{
\item \code{petfit_generated_by()}: The \code{GeneratedBy} entry to write into a
sidecar, following the BIDS field of the same name.

\item \code{petfit_check_version()}: Warn if \code{provenance} was written by an
older petfit than the one running, or records no version at all.

\item \code{petfit_check_combined_tacs_version()}: Check the sidecar beside a combined
TACs file, if there is one. Silent when the file has no sidecar to check.

}}
