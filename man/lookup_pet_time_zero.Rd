% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/region_utils.R
\name{lookup_pet_time_zero}
\alias{lookup_pet_time_zero}
\title{Look Up a Measurement's TimeZero in a Parsed BIDS Study}
\usage{
lookup_pet_time_zero(study_data, attributes)
}
\arguments{
\item{study_data}{Parsed study, as returned by \code{\link[kinfitr:bids_parse_study]{kinfitr::bids_parse_study()}},
or \code{NULL}.}

\item{attributes}{One row of BIDS attributes, as returned by
\code{\link[=extract_bids_attributes_from_filename]{extract_bids_attributes_from_filename()}}.}
}
\value{
The \code{TimeZero} string, or \code{NA_character_} when it cannot be found.
}
\description{
Read the BIDS \code{TimeZero} of one acquisition from the raw
\verb{_pet.json} metadata that \code{\link[kinfitr:bids_parse_study]{kinfitr::bids_parse_study()}} has already
collected, matching on whichever entities the measurement carries.

\code{TimeZero} is what lets the runs of one measurement be placed on a common
clock. It lives in the raw sidecars, and the preprocessing pipelines
generally do not carry it into their \verb{_tacs.json}, so a run given only a
derivatives directory usually has none -- which is handled by taking the
frame times as already shared. See \code{\link[=merge_tacs_runs]{merge_tacs_runs()}}.
}
