% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tstar_finder.R
\name{run_tstar_finder}
\alias{run_tstar_finder}
\title{Run the t* finder and save diagnostic plots}
\usage{
run_tstar_finder(
  analysis_folder,
  config_type,
  model,
  high_region,
  med_region,
  low_region,
  selection = c("random", "all", "subset"),
  sub_filter = "",
  ses_filter = "",
  n_random = 5L,
  k2prime = 0.1,
  bids_dir = NULL,
  blood_dir = NULL,
  progress = NULL
)
}
\arguments{
\item{analysis_folder}{Path to the analysis folder.}

\item{config_type}{Either "plasma input" or "reference tissue".}

\item{model}{Model to run the t* finder for. Plasma: "Logan", "MA1", "Patlak".
Reference: "MRTM1", "refLogan", "MRTM2".}

\item{high_region, med_region, low_region}{Region names with high/medium/low binding.}

\item{selection}{One of "random" (default), "all", or "subset".}

\item{sub_filter, ses_filter}{Semicolon-separated sub/ses values used when
\code{selection == "subset"}.}

\item{n_random}{Number of measurements to sample when \code{selection == "random"}.}

\item{k2prime}{k2prime value for refLogan/MRTM2 (default 0.1).}

\item{bids_dir, blood_dir}{Optional BIDS and blood directories (plasma only).}

\item{progress}{Optional callback \verb{function(fraction, message)} for a progress bar.}
}
\value{
Character vector of written PNG file paths (invisibly on success).
}
\description{
Generates kinfitr t* (t-star) diagnostic plots for a chosen linear model and a
chosen set of PET measurements, to help decide on a t* value. Unlike the other
petfit steps this does \strong{not} produce an HTML report: it writes one PNG per
measurement to \verb{reports/tstar_finder/\{model\}/\{measurement\}_tstar.png} inside the
analysis folder.

The internal fits are run unweighted, mirroring kinfitr's \verb{*_tstar} functions
(which take no weights). For plasma models the blood delay is read from the
\verb{*_desc-delayfit_kinpar.tsv} files (defaulting to 0 when absent) and \code{vB} is
fixed at 0.05. For \code{refLogan}/\code{MRTM2} the user supplies \code{k2prime}.
}
