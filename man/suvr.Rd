% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/suvr.R
\name{suvr}
\alias{suvr}
\title{Standardised uptake value and uptake value ratio}
\usage{
suvr(
  t_tac,
  reftac,
  roitac,
  dur,
  frame_start = NULL,
  frame_end = NULL,
  injRad = NA_real_,
  bodymass = NA_real_,
  frameStartEnd = NULL,
  timeStartEnd = NULL
)
}
\arguments{
\item{t_tac}{Numeric vector of frame midpoint times, in minutes.}

\item{reftac}{Numeric vector of radioactivity concentrations in the reference
region for each frame.}

\item{roitac}{Numeric vector of radioactivity concentrations in the target
region for each frame.}

\item{dur}{Numeric vector of frame durations, in minutes. Used for the
integral, so that it is a frame-duration weighted sum rather than a
trapezoidal approximation.}

\item{frame_start, frame_end}{Optional numeric vectors of frame start and end
times in minutes, used to report the window bounds. Defaults to the range of
the midpoints of the included frames.}

\item{injRad}{Injected radioactivity. \code{NA} (the default) means no dose
information is available, and the SUV outcomes are returned as \code{NA}.}

\item{bodymass}{Body mass of the participant. \code{NA} (the default) means it is
unavailable; see the \code{bodymass_default} argument of \code{\link[=suv_denominator]{suv_denominator()}} for
the 70 kg approximation used elsewhere in petfit.}

\item{frameStartEnd}{Optional length-2 vector giving the first and last frame
number to include, e.g. \code{c(1, 20)}. Either element may be \code{NA} or \code{NULL} for
a one-sided window.}

\item{timeStartEnd}{Optional length-2 vector giving the start and end time in
minutes. Frames whose midpoint lies within the window are included. Either
element may be \code{NA} for a one-sided window.}
}
\value{
An object of class \code{suvr}: a list with \code{par} (a one-row tibble of
outcomes), \code{tacs} (the input TACs annotated with which frames were included)
and \code{window} (the resolved frame indices). \code{par} contains:
\describe{
\item{SUVR}{Target integral divided by reference integral.}
\item{SUV, SUV_ref}{Mean SUV over the window; \code{NA} without \code{injRad}.}
\item{intSUV, intSUV_ref}{SUV integral; \code{NA} without \code{injRad}.}
\item{meanTAC, meanTAC_ref}{Frame-duration weighted mean radioactivity
concentration over the window.}
\item{intTAC, intTAC_ref}{Radioactivity concentration integral.}
\item{window_start, window_end, window_duration, n_frames}{The window that
was actually integrated.}
}
}
\description{
Estimates the standardised uptake value (SUV) and the target-to-reference
standardised uptake value ratio (SUVR) over a frame or time window, following
the regional TAC ratio approach of Turku PET Centre's \code{dftratio}.

The integrals come from \code{\link[kinfitr:SUV]{kinfitr::SUV()}}, so the window is resolved exactly as
kinfitr resolves it: \code{timeStartEnd} selects whole frames whose \strong{midpoint}
falls inside the window, never a partial frame. The window is normalised here
rather than left to kinfitr so that a one-sided window (only a start, or only
an end) is well defined, and so that the reported window bounds and duration
describe the frames that were actually integrated.

SUVR is a ratio of two integrals over the same frames, so the injected
radioactivity and body mass cancel: it is identical whether it is computed
from radioactivity concentrations or from SUV. SUV itself is not, and is
returned as \code{NA} unless \code{injRad} is supplied.
}
\examples{
\dontrun{
suvr(t_tac = tac$frame_mid, reftac = tac$RefTAC, roitac = tac$TAC,
     dur = tac$frame_dur, timeStartEnd = c(60, 90),
     injRad = 180, bodymass = 75)
}

}
