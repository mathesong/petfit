% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/suvr.R
\name{suv_denominator}
\alias{suv_denominator}
\title{Resolve the SUV denominator for a set of measurements}
\usage{
suv_denominator(inj_rad, bodyweight, bodymass_default = 70)
}
\arguments{
\item{inj_rad}{Numeric vector of injected radioactivities.}

\item{bodyweight}{Numeric vector of body weights.}

\item{bodymass_default}{Body mass in kg to assume when body weight is missing
but the dose is known. Defaults to 70.}
}
\value{
A list with \code{mode} (one of \code{"SUV"}, \code{"SUV_bw70"} or \code{"none"}),
\code{bodymass} (the vector to use, or \code{NA}) and \code{label}, a sentence describing
the choice for the report.
}
\description{
Decides how SUV should be calculated for a set of measurements, following the
same rule the data definition report uses: a true SUV when injected
radioactivity and body weight are known for every measurement, a body weight
of 70 kg assumed when only the dose is known, and no SUV at all when the dose
is missing anywhere.

The decision is deliberately made across all measurements rather than per
measurement, so that an outcome column means the same thing for every row of
the cohort.
}
