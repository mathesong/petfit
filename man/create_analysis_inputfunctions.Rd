% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/blood_utils.R
\name{create_analysis_inputfunctions}
\alias{create_analysis_inputfunctions}
\title{Create Input Functions in the Analysis Folder from Raw BIDS Blood Data}
\usage{
create_analysis_inputfunctions(
  bids_dir,
  tac_data,
  analysis_folder,
  interp_points = 6000
)
}
\arguments{
\item{bids_dir}{Path to the raw BIDS directory.}

\item{tac_data}{Tibble of loaded TAC data, carrying entity columns and the
\code{filename} of each measurement's TACs file relative to \code{analysis_folder}.}

\item{analysis_folder}{Path to the analysis folder to write into.}

\item{interp_points}{Number of points to interpolate each curve over.}
}
\value{
List with \code{files} (the input function paths written) and \code{messages}
(character vector describing what was done, for the caller to report).
}
\description{
Build an \verb{_inputfunction.tsv} file, with its JSON sidecar, for
each measurement in \code{tac_data}, by linear interpolation of the blood curves
in the raw BIDS \verb{_blood.tsv} files. This is the fallback used when no
processed input functions are available from bloodstream.

Measurements are matched to raw acquisitions on whichever BIDS entities the
TACs carry. When the runs were merged in the region definition step the
TACs no longer carry \code{run}, so one measurement matches every run of that
acquisition, and their input functions are pooled into one curve by
\code{\link[=merge_inputfunction_tables]{merge_inputfunction_tables()}} before being written. The output is named
after the TACs file, so a merged measurement gets a single input function
with no run entity, which then joins to its TACs.
}
