% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validation.R
\name{validate_min_regions_per_pet}
\alias{validate_min_regions_per_pet}
\title{Validate the Number of Regions per PET Measurement}
\usage{
validate_min_regions_per_pet(data, min_regions = 2)
}
\arguments{
\item{data}{A data frame with \code{pet} and \code{region} columns, one row per
(measurement, region) or per (measurement, region, frame).}

\item{min_regions}{Minimum number of distinct regions a measurement needs.
Default is 2.}
}
\value{
A tibble with columns \code{pet}, \code{n_regions} and \code{sufficient}. Errors if
no measurement has at least \code{min_regions} regions; warns (naming the
measurements) if some do not.
}
\description{
Nested models fit all regions of a PET measurement jointly with
shared parameters, so a measurement with a single region cannot be fitted:
there is nothing to share across. This check summarises how many regions
each measurement carries, warns about measurements that fall below the
minimum (so the caller can drop them), and errors if no measurement
reaches the minimum.
}
