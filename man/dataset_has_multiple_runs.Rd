% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/region_utils.R
\name{dataset_has_multiple_runs}
\alias{dataset_has_multiple_runs}
\title{Does This Dataset Have More Than One Run per Measurement?}
\usage{
dataset_has_multiple_runs(derivatives_folder)
}
\arguments{
\item{derivatives_folder}{Character string path to the derivatives folder.}
}
\value{
\code{TRUE} when at least one measurement has two or more runs.
}
\description{
Report whether any measurement in a derivatives folder is
represented by more than one run.

Used to decide whether the run merging option is worth putting in front of
a user. The mere presence of a \code{run} entity is not enough: a dataset
labelled \code{run-01} throughout has nothing to merge, so the question is
whether any one measurement has several runs.

Read from the filenames rather than from the TACs themselves, so it costs
a directory listing and can be answered before the app opens.
}
\examples{
\dontrun{
dataset_has_multiple_runs("/path/to/derivatives")
}
}
