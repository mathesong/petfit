% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bids_utils.R
\name{pet_label}
\alias{pet_label}
\title{Display Label for a PET Measurement}
\usage{
pet_label(keys)
}
\arguments{
\item{keys}{Character vector of keys, as returned by \code{\link[=pet_key]{pet_key()}}.}
}
\value{
Character vector of labels. Never use these as values.
}
\description{
Shorten a set of keys for display by dropping the entities they
all share.

This is deliberately cohort-dependent, which is safe only because it is
never persisted: labels are shown to a user, while \code{\link[=pet_key]{pet_key()}} is what goes
into filenames, joins and saved configurations. Making \emph{identity}
cohort-dependent is what orphaned files whenever a dataset grew.
}
\examples{
pet_label(c("sub-01_ses-test", "sub-02_ses-test"))
}
