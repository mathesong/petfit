% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/debugging.R
\name{petfit_error_detail}
\alias{petfit_error_detail}
\title{Full Detail of an Error, Including Its Causes}
\usage{
petfit_error_detail(e)
}
\arguments{
\item{e}{A condition.}
}
\value{
A single character string: the message, then each cause beneath it.
}
\description{
Render an error's message together with every cause beneath it.

dplyr and purrr report where a failure happened and put \emph{what} failed in a
chained parent condition. Printing only \code{conditionMessage(e)} therefore
yields "In argument: \code{mappings = purrr::map(path, create_tacs_morph_mapping)}"
and nothing about the cause, which is unactionable: it names the line that
was running, not the problem. This walks the chain so the message a user
sees ends at the actual cause.
}
\examples{
\dontrun{
tryCatch(some_dplyr_pipeline(), error = function(e) cat(petfit_error_detail(e)))
}
}
