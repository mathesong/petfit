% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/blood_utils.R
\name{merge_inputfunction_tables}
\alias{merge_inputfunction_tables}
\title{Merge the Input Functions of Several Runs into One}
\usage{
merge_inputfunction_tables(
  tables,
  interp_points = 6000,
  label = "this measurement",
  offsets = NULL
)
}
\arguments{
\item{tables}{List of tibbles from \code{\link[=blooddata_inputfunction_table]{blooddata_inputfunction_table()}}, one per
run, each carrying a \code{measured_range} attribute.}

\item{interp_points}{Number of points in the merged grid.}

\item{label}{How to name this measurement in error messages.}

\item{offsets}{Seconds to add to each table's times, so that runs which each
count from their own \code{TimeZero} are placed on one clock -- the same
alignment \code{\link[=merge_tacs_runs]{merge_tacs_runs()}} applies to the frame times, and bloodstream
to these same samples. \code{NULL} (the default) leaves the times as they are,
for runs already sharing a time zero.}
}
\value{
A single tibble with the same columns as its inputs.
}
\description{
Pool the input functions of the runs of a single measurement
into one curve, for datasets where those runs are consecutive scans of one
injection.

Deliberately plain: each run's curve is interpolated on its own, the pieces
are laid end to end, and the result is interpolated once more onto a single
grid. Nothing is modelled, and the gap between the runs -- when the subject
was out of the scanner and, usually, unsampled -- is bridged linearly like
any other interval between samples.

Each run contributes only the part of its curve that rests on measured
samples. \code{\link[kinfitr:bd_create_input]{kinfitr::bd_create_input()}} always interpolates from time zero,
so a second run's table opens with a long stretch of padding standing in
for the first run's real data; keeping it would let that padding overwrite
the first run.
}
