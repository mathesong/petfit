% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bids_utils.R
\name{bids_hierarchical_inner_join}
\alias{bids_hierarchical_inner_join}
\title{Join BIDS tables with hierarchical matching for missing right-hand entities}
\usage{
bids_hierarchical_inner_join(
  x,
  y,
  entity_cols = c("sub", "ses", "trc", "rec", "task", "run")
)
}
\arguments{
\item{x}{Left table, usually PET TAC data.}

\item{y}{Right table, usually blood/input-function data.}

\item{entity_cols}{BIDS entity columns to use for matching.}
}
\value{
A tibble with one best matching \code{y} row per matched \code{x} row.
}
\description{
Joins a PET-level table (\code{x}) to an ancillary table (\code{y}) while allowing
missing entities in \code{y} to match more specific rows in \code{x}. This supports
common BIDS layouts such as run-specific PET files with a session-level blood
file that has no \code{run} entity. If a parser has expanded a generic file into
specific entity rows, missing entities in \code{x} can also fall back to those
specific rows, while true generic matches are preferred.
}
